extends layouts/layout.pug
include mixins/movie.pug

block content
    form(action="/search")
        input(type="text", placeholder="Search by Title", name="searchTitle", required)
        input(type="submit", value="search")
    div
        each movie in movies
            +movie(movie)